# Maven command-line options for consistent builds
# This file contains Maven CLI options applied to all builds

# Batch mode (non-interactive)
-B

# Show version info
-V

# Define Java version for compilation
-Djava.version=17
-Dmaven.compiler.source=17
-Dmaven.compiler.target=17